language:
  - java

jdk:
  - oraclejdk7

services:
  - mysql

install:
  - rvm install 1.8.7

before_script:
  - export RAILS_ENV=development
  - test/select-dbms.sh mysql

after_success:
  - bash <(curl -s https://codecov.io/bash)
